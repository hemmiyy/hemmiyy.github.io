<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>hemmiyy | Links</title>
  <style>
    :root {
      --card-bg: rgba(15, 23, 42, 0.88);
      --accent: #38bdf8;
      --text-main: #e5e7eb;
      --text-muted: #9ca3af;
      --radius-xl: 18px;
      --radius-lg: 999px;
      --shadow-soft: 0 18px 45px rgba(0, 0, 0, 0.7);
      --transition-fast: 0.18s ease-out;
      --font-main: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
        "Segoe UI", sans-serif;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      min-height: 100vh;
      font-family: var(--font-main);
      color: var(--text-main);
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 24px;

      /* 🔥 Full-screen Naruto background */
      background-image:
        linear-gradient(rgba(0, 0, 0, 0.55), rgba(0, 0, 0, 0.85)),
        url("naruto-bg.jpg");
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }

    .page {
      width: 100%;
      max-width: 460px;
    }

    .card {
      background: var(--card-bg);
      border-radius: 24px;
      padding: 22px 20px 20px;
      border: 1px solid rgba(148, 163, 184, 0.25);
      box-shadow: var(--shadow-soft);
      backdrop-filter: blur(18px);
    }

    .profile {
      display: flex;
      align-items: center;
      gap: 16px;
      margin-bottom: 18px;
    }

    .avatar {
      width: 64px;
      height: 64px;
      border-radius: 50%;
      background: radial-gradient(circle at 30% 10%, #f9fafb, #64748b);
      border: 2px solid rgba(148, 163, 184, 0.6);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 26px;
      color: #020617;
      flex-shrink: 0;
    }

    .profile-text h1 {
      font-size: 1.15rem;
      letter-spacing: 0.02em;
      margin-bottom: 3px;
      text-transform: lowercase;
    }

    .profile-text p {
      font-size: 0.86rem;
      color: var(--text-muted);
      line-height: 1.4;
    }

    .tagline {
      margin-bottom: 14px;
      font-size: 0.8rem;
      color: var(--accent);
      text-transform: uppercase;
      letter-spacing: 0.22em;
    }

    .social-row {
      display: flex;
      gap: 8px;
      margin-bottom: 18px;
      flex-wrap: wrap;
    }

    .pill {
      border-radius: var(--radius-lg);
      border: 1px solid rgba(148, 163, 184, 0.4);
      padding: 6px 12px;
      font-size: 0.78rem;
      color: var(--text-muted);
      display: inline-flex;
      align-items: center;
      gap: 6px;
      background: rgba(15, 23, 42, 0.9);
      white-space: nowrap;
    }

    .pill-dot {
      width: 7px;
      height: 7px;
      border-radius: 999px;
      background: var(--accent);
      box-shadow: 0 0 0 4px rgba(56, 189, 248, 0.18);
    }

    .links {
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-bottom: 14px;
    }

    .link-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      border-radius: var(--radius-xl);
      padding: 12px 14px;
      border: 1px solid rgba(148, 163, 184, 0.35);
      background: radial-gradient(circle at 0% 0%, rgba(56, 189, 248, 0.12), transparent 60%),
                  rgba(15, 23, 42, 0.96);
      text-decoration: none;
      color: var(--text-main);
      font-size: 0.92rem;
      box-shadow: 0 10px 28px rgba(15, 23, 42, 0.95);
      transition:
        transform var(--transition-fast),
        box-shadow var(--transition-fast),
        border-color var(--transition-fast),
        background var(--transition-fast);
    }

    .link-item:hover {
      transform: translateY(-1px);
      box-shadow: 0 18px 35px rgba(15, 23, 42, 1);
      border-color: var(--accent);
      background:
        linear-gradient(135deg, rgba(56, 189, 248, 0.18), rgba(15, 23, 42, 0.98));
    }

    .link-main {
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .link-title {
      font-weight: 500;
    }

    .link-subtitle {
      font-size: 0.78rem;
      color: var(--text-muted);
    }

    .link-icon {
      font-size: 1.1rem;
      margin-left: 10px;
      opacity: 0.8;
    }

    .footer {
      margin-top: 6px;
      text-align: center;
      font-size: 0.75rem;
      color: var(--text-muted);
    }

    .footer a {
      color: var(--accent);
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    @media (max-width: 480px) {
      .card {
        padding: 18px 16px 18px;
        border-radius: 20px;
      }

      .profile {
        gap: 12px;
      }

      .avatar {
        width: 56px;
        height: 56px;
        font-size: 22px;
      }
    }
  </style>
</head>
<body>
  <main class="page">
    <section class="card" aria-label="Link in bio">
      <div class="profile">
        <!-- Avatar initials -->
        <div class="avatar">H</div>
        <div class="profile-text">
          <h1>hemmiyy</h1>
          <p>Content creator</p>
        </div>
      </div>

      <div class="tagline">All my links in one place</div>

      <div class="social-row">
        <span class="pill">
          <span class="pill-dot"></span>
          Open for collaborations
        </span>
        <span class="pill">@hemmiyy</span>
      </div>

      <div class="links" aria-label="Important links">
        <!-- YouTube -->
        <a class="link-item" href="https://youtube.com/@hemmiyy?si=qo8nHptknVSfRUX8" target="_blank" rel="noopener">
          <div class="link-main">
            <span class="link-title">YouTube</span>
            <span class="link-subtitle">Watch my latest videos</span>
          </div>
          <span class="link-icon">▶</span>
        </a>

        <!-- TikTok -->
        <a class="link-item" href="https://www.tiktok.com/@hemmiyy" target="_blank" rel="noopener">
          <div class="link-main">
            <span class="link-title">TikTok</span>
            <span class="link-subtitle">Short clips & trending content</span>
          </div>
          <span class="link-icon">🎵</span>
        </a>

        <!-- Snapchat -->
        <a class="link-item" href="https://www.snapchat.com/add/Hemmi_btw" target="_blank" rel="noopener">
          <div class="link-main">
            <span class="link-title">Snapchat</span>
            <span class="link-subtitle">Add me on Snap: Hemmi_btw</span>
          </div>
          <span class="link-icon">👻</span>
        </a>
      </div>

      <p class="footer">
        © <span id="year"></span> hemmiyy · Made with ❤️
      </p>
    </section>
  </main>

  <script>
    // Auto-set current year in footer
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>
</body>
</html>